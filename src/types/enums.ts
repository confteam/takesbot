export enum BotType {
  TAKES = "TAKES",
  MP = "MP",
  MOD = "MOD",
}

export enum UserRole {
  MEMBER = "MEMBER",
  BANNED = "BANNED",
  ADMIN = "ADMIN",
  SUPERADMIN = "SUPERADMIN"
}

export enum AnonimityPayload {
  ANON = "anon",
  NOTANON = "notanon",
}

export enum TakeStatus {
  PENDING = "PENDING",
  ACCEPTED = "ACCEPTED",
  REJECTED = "REJECTED"
}
